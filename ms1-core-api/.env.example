# Server
PORT=5000
NODE_ENV=development

# PostgreSQL (Supabase)
DATABASE_URL=postgresql://user:password@host:port/database

# JWT (used in Phase 03)
JWT_SECRET=your_jwt_secret_here

# Redis (used in Phase 06)
REDIS_URL=redis://localhost:6379

# MS2 Internal URL (used in Phase 07)
MS2_BASE_URL=http://localhost:7000

# Shared webhook secret — must match MS2_WEBHOOK_SECRET in ms2-agent/.env (Phase 09+)
MS2_WEBHOOK_SECRET=change_me_in_production
